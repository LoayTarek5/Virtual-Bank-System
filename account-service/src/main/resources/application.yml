server:
  port: 8082

spring:
  application:
    name: account-service
  datasource:
    url: jdbc:postgresql://localhost:5432/vbank_accounts
    username: vbank
    password: vbank
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false